apps:

  - name: import
    script: ./scripts/importOrderTemplates.babel.js
    watch: false
    env:
      NODE_ENV: production

  - name: notifications
    script: ./src/services/notifications/instance.babel.js
    watch: false
    env:
      NODE_ENV: production
